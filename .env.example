# Keep this server-side. Never prefix it with NEXT_PUBLIC_.
PANTA_API_KEY=pk_test_replace_me
PANTA_API_BASE_URL=https://live-api.panta.market/api/v1

# Public RPC endpoint used by connected wallets to broadcast signed transactions.
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# Optional server-side RPC override for supply and market-cap calculations.
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
